# Railway环境变量配置示例
# 在Railway Dashboard中设置这些变量

# ========================================
# 🗄️ 数据库配置 (Railway MySQL插件自动提供)
# ========================================
# 这些变量由Railway MySQL服务自动生成，无需手动设置
DB_HOST=${MYSQLHOST}
DB_USER=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}
DB_NAME=${MYSQLDATABASE}
DB_PORT=${MYSQLPORT}

# 留言板数据库配置（使用相同的MySQL数据库）
MYSQL_HOST=${MYSQLHOST}
MYSQL_USER=${MYSQLUSER}
MYSQL_PASSWORD=${MYSQLPASSWORD}
MYSQL_DATABASE=${MYSQLDATABASE}
MYSQL_PORT=${MYSQLPORT}

# ========================================
# 🌐 服务器配置 (Railway自动提供)
# ========================================
# Railway会自动设置端口，无需手动配置
PORT=${PORT}
NODE_ENV=production

# ========================================
# 🔒 安全配置
# ========================================
# 速率限制配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 管理员API密钥 (请修改为您的密钥)
ADMIN_API_KEY=EpZRllhApFMUenjfLOyXSilDPHFyGbPg

# 管理员令牌 (用于管理后台登录)
ADMIN_TOKEN=admin123

# ========================================
# 🌍 可选：自定义配置
# ========================================
# CORS跨域设置 (如果需要)
# CORS_ORIGIN=https://yourdomain.com

# 日志级别
# LOG_LEVEL=info

# 调试模式
# ENABLE_DEBUG=false

# 时区设置
TZ=Asia/Shanghai
